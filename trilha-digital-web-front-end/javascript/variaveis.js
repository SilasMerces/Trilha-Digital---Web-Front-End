// Variáveis
// Tipos primitivos de Variáveis
// Pedacinhos de memórias que reservamos no computador.

let idade = 30 //Criando a primeira variável

console.log (idade)

idade = 25            // Variável tipo NUMBER
let numero = 30      // Variável tipo NUMBER
let altura = 1.80    // Variável tipo NUMBER
let nome = 'Silas'   // Variável tipo STRING (Texto)
let estudando = true  // Variável tipo BOLEANO (Verdadeiro ou Falso)


console.log(numero)
console.log(altura)
console.log(nome)
console.log(estudando)

let outra; //Se não dermos um valor para variavel, ela recebe Undefined

const constante = 100 // "Variável" constante não pode alterar o valor dela, por isso "const" de constante